---
interface Props {
  title: string;
  sub?: boolean;
}

const { title, sub = false } = Astro.props;
---

<div class="mb-5 min-w-full">
  <span class={`${!sub ? "text-2xl font-extrabold" : "text-xl"} font-bold`}
    >{title}</span
  >
  <slot />
</div>
